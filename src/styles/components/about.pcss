.about {
  position: relative;
  height: 100vh;
  min-height: 650px;
  padding-top: 195px;
}
.about__container {
  display: flex;
  max-width: 980px;
}
.about__info {
  flex: 2;
  margin-right: 65px;
}

.about__desc {
  flex: 3;
  display: flex;
  flex-direction: column;
}
.about__welcome {
  margin-bottom: $margin-safe;
}
.about__title {
  margin-bottom: 28px;
}
.about__text {
}
.about__ps {
  margin-top: auto;
}
