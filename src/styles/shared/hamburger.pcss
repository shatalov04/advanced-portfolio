.hamburger {
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: $delay;
  &:hover {
    color: $main-color;
  }
}
.hamburger__icon {
  width: 32px;
  height: 32px;
  display: block;
  fill: currentColor;
}
